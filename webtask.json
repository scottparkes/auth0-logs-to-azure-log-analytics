{
  "title": "Logs to Azure Log Analytics",
  "name": "logs-to-azure-log-analytics",
  "version": "1.0.0",
  "author": "dpiessens",
  "description": "Logs and export them to Azure Log Analytics",
  "type": "cron",
  "keywords": [
    "dpiessens",
    "extension"
  ],
  "schedule": "0 */5 * * * *",
  "auth0": {
    "scopes": "read:logs"
  },
  "secrets": {
    "BATCH_SIZE": {
      "description": "The ammount of logs to be read on each execution. Maximum is 100.",
      "default": 100
    },
    "LOG_ANALYTICS_WORKSPACE_ID": {
      "description": "Azure Log Analytics Workspace ID",
      "required": true
    },
    "LOG_ANALYTICS_SHARED_KEY": {
      "description": "Azure Log Analytics Workspace Shared Key",
      "required": true
    }
  }
}
